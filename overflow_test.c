#include <stdio.h>
#include <stdint.h>

int main(void) {
    int32_t larger_value = 32768; // This value exceeds the maximum for int16_t
    int16_t smaller_value = (int16_t)larger_value; // What is "(int16_t)larger_value"? It is a type cast that converts the larger_value from int32_t to int16_t. Why not just "int16_t smaller_value = -32769"? Because -32769 is outside the range of int16_t. But that's what I want to test, right? Why can I "int32_t larger_value = 32768" but not "int16_t smaller_value = -32769"? Because 32768 is within the range of int32_t, which can hold values from -2,147,483,648 to 2,147,483,647. However, -32769 is outside the range of int16_t, which can only hold values from -32,768 to 32,767. The type cast allows you to assign a value that exceeds the range of int16_t, but it will result in an overflow and produce an incorrect value. If 32768 is whithin the range of int32_t, then what "overflow_test" is being tested? If 32768 is whitin the range, then it won't overflow, right? The overflow test is being tested when we cast the larger_value (32768) to int16_t. Since 32768 exceeds the maximum value for int16_t (32,767), it will cause an overflow when we perform the type cast. So this test is for the case in which i'd use int16_t while defining the maximum amplitude as 32768, right? Yes, that's correct. The test is to demonstrate what happens when you try to assign a value that exceeds the range of int16_t (in this case, 32768) to a variable of type int16_t. It will result in an overflow and produce an incorrect value. What is the expected output of this program? The expected output of this program is that the smaller_value will not hold the correct value of 32768 due to the overflow. Instead, it will wrap around and produce a negative value. Specifically, it will output -32768, which is the result of the overflow when casting 32768 to int16_t.
    printf("32768 cast to int16_t results in: %d\n", smaller_value);
    return 0;
}